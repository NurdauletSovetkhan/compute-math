# Assignment 5: Numerical Differentiation

Реализация численного дифференцирования на основе учебника (разделы 2-4).

## 📁 Структура проекта

```
assignment-5/
├── main.py                          # Главный файл с меню
├── section2_equally_spaced.py       # Раздел 2: Равномерно расположенные точки
├── section3_unequally_spaced.py     # Раздел 3: Неравномерно расположенные точки
├── section4_extrema_analysis.py     # Раздел 4: Анализ экстремумов
├── diferentation.py                 # Исходный монолитный файл (legacy)
└── README.md                        # Эта документация
```

## 📚 Разделы (по учебнику)

### Section 2: Derivatives using equally spaced values
**Файл:** `section2_equally_spaced.py`

- **2.1** Численное дифференцирование формулой Ньютона вперед
- **2.2** Численное дифференцирование формулой Ньютона назад
- Центральные разности для внутренних точек
- Таблицы конечных разностей (forward & backward)

**Методы:**
- `newton_forward_derivative()` - производная по формуле Ньютона вперед
- `newton_backward_derivative()` - производная по формуле Ньютона назад
- `central_difference()` - центральная разность
- `forward_difference_table()` - таблица конечных разностей вперед
- `backward_difference_table()` - таблица конечных разностей назад

### Section 3: Derivatives using unequally spaced values
**Файл:** `section3_unequally_spaced.py`

- **3.1** Неравномерные конечные разности вперед
- **3.2** Неравномерные конечные разности назад
- Центрально-подобный метод для неравномерных данных

**Методы:**
- `unequally_spaced_forward()` - формула вперед (3 точки)
- `unequally_spaced_backward()` - формула назад (3 точки)
- `unequally_spaced_central()` - центральная формула (3 точки)

### Section 4: Analysis of tabulated functions
**Файл:** `section4_extrema_analysis.py`

- **4.1** Максимумы и минимумы табулированной функции
- **4.2** Тест первой производной для экстремумов
- **4.3** Тест второй производной для экстремумов

**Методы:**
- `find_extrema()` - поиск локальных экстремумов
- `first_derivative_test()` - тест первой производной (f'(x) ≈ 0)
- `second_derivative_test()` - тест второй производной (f''(x) > 0 или < 0)

## 🚀 Запуск программы

### Основной файл с меню:
```bash
python main.py
```

### Меню программы:
```
1. Run All Sections (Complete Analysis) - Полный анализ всех разделов
2. Section 2: Equally Spaced Differentiation - Только раздел 2
3. Section 3: Unequally Spaced Differentiation - Только раздел 3
4. Section 4: Extrema Analysis - Только раздел 4
5. Exit - Выход
```

### Запуск отдельных разделов (в коде):
```python
from section2_equally_spaced import run_section2
from section3_unequally_spaced import run_section3
from section4_extrema_analysis import run_section4

# Раздел 2
run_section2(x_values, f_values, h)

# Раздел 3
run_section3(x_values, f_values)

# Раздел 4
run_section4(x_values, f_values, h, is_equally_spaced)
```

## 📥 Примеры ввода данных

### Пример 1: Равномерно расположенные точки
```
Enter x values: 0 0.5 1.0 1.5 2.0
Enter function: x**2
Compute from function: 2
```

### Пример 2: Неравномерно расположенные точки
```
Enter x values: 0 0.3 0.8 1.5 2.1
Enter function: sin(x)
Compute from function: 2
```

### Пример 3: Ручной ввод f(x)
```
Enter x values: 1 2 3 4 5
Enter function: (can be anything)
Enter f(x) values manually: 1
Enter f(x) values: 1 4 9 16 25
```

## 🧮 Математические формулы

### Newton's Forward Difference (2.1):
```
f'(x₀) ≈ (1/h)[Δf₀ - (1/2)Δ²f₀ + (1/3)Δ³f₀ - ...]
f''(x₀) ≈ (1/h²)[Δ²f₀ - Δ³f₀ + (11/12)Δ⁴f₀ - ...]
```

### Newton's Backward Difference (2.2):
```
f'(xₙ) ≈ (1/h)[∇fₙ + (1/2)∇²fₙ + (1/3)∇³fₙ + ...]
f''(xₙ) ≈ (1/h²)[∇²fₙ + ∇³fₙ + (11/12)∇⁴fₙ + ...]
```

### Central Difference:
```
f'(xᵢ) ≈ (f(xᵢ₊₁) - f(xᵢ₋₁)) / (2h)
f''(xᵢ) ≈ (f(xᵢ₊₁) - 2f(xᵢ) + f(xᵢ₋₁)) / h²
```

### Unequally Spaced (3.1, 3.2):
Использует интерполяционные полиномы Лагранжа для 3 точек.

### Extrema Tests (4.2, 4.3):
- **First derivative test**: f'(x) ≈ 0 в критической точке
- **Second derivative test**: 
  - f''(x) > 0 → минимум (вогнутость вверх)
  - f''(x) < 0 → максимум (вогнутость вниз)

## 🔧 Требования

```python
import math
import sys
import os
```

Стандартная библиотека Python 3.x - дополнительные пакеты не требуются.

## 📊 Выходная информация

Программа выводит:
1. **Таблицу данных** - исходные x и f(x)
2. **Таблицы конечных разностей** (для равномерных данных)
3. **Производные** во всех точках:
   - Первая производная f'(x)
   - Вторая производная f''(x)
4. **Анализ экстремумов**:
   - Найденные максимумы и минимумы
   - Результаты теста первой производной
   - Результаты теста второй производной
   - Верификация результатов

## 📝 Примечания

- Для численного дифференцирования требуется **минимум 3 точки**
- Section 2 лучше работает с **равномерными** данными
- Section 3 работает с **любыми** данными, но оптимизирован для неравномерных
- Section 4 автоматически определяет тип данных и выбирает подходящие методы

## 🔄 Миграция со старого файла

Старый монолитный файл `diferentation.py` сохранен для обратной совместимости. Используйте новую модульную структуру для лучшей организации кода:

```python
# Старый способ
# python diferentation.py

# Новый способ
python main.py
```

## 👨‍💻 Автор

Assignment 5 - Numerical Differentiation  
Compute Math Course
